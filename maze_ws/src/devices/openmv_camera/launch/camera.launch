<launch>

    <arg name="rpc-path" default="$(find openmv_camera)/scripts"/>
    <arg name="port" default="/dev/ttyACM0"/>
    <arg name="debug" default="true"/>

    <!-- Launch camera service -->
    <node name="camera_service" pkg="openmv_camera" type="camera_controller.py" respawn="true" output="screen">
        <param name="debug" value="$(arg debug)"/>
        <param name="port" value="$(arg port)"/>
        <env name="PYTHONPATH" value="$(eval arg('rpc-path') + ':' + optenv('PYTHONPATH'))"/>
    </node>
</launch>